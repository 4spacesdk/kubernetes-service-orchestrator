<?php namespace App\Controllers;

use App\Core\ResourceController;

/**
 * The counts of earlier scans, for a graph over time. Read only: the scanner writes them.
 */
class ContainerImageScanRecords extends ResourceController {

}
